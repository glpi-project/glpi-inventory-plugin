{% extends "generic_show_form.html.twig" %}
{% import 'components/form/fields_macros.html.twig' as fields %}
{% set params  = params ?? [] %}

{% block more_fields %}
   {{ fields.textField(
      'url',
      item.fields['url'],
      __('Mirror server address', 'glpiinventory'),
   ) }}
{% endblock %}
